<Window x:Class="SIAP.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SIAP Foundation" Height="600" Width="900">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
      <Button x:Name="OpenProjectButton"
              Content="Open Project"
              Width="140"
              Margin="0,0,12,0"
              Click="OnOpenProjectClicked" />
      <Button x:Name="GenerateExportButton"
              Content="Generate Export"
              Width="160"
              IsEnabled="False"
              Click="OnGenerateExportClicked" />
    </StackPanel>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="2*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <GroupBox Header="Files" Margin="0,0,12,0">
        <ListBox x:Name="FilesListBox" />
      </GroupBox>

      <GroupBox Grid.Column="1" Header="Log">
        <TextBox x:Name="LogTextBox"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />
      </GroupBox>
    </Grid>

    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
      <TextBlock Text="File Count:" FontWeight="SemiBold" />
      <TextBlock x:Name="FileCountText" Margin="6,0,16,0" Text="0" />
      <TextBlock Text="Total Size (bytes):" FontWeight="SemiBold" />
      <TextBlock x:Name="TotalSizeText" Margin="6,0,0,0" Text="0" />
    </StackPanel>
  </Grid>
</Window>
